# Local Postgres database (Docker container on host, port 5432)
# Copy this file to .env and fill in real values.
ConnectionStrings__Default=Host=host.docker.internal;Port=5432;Database=postgres;Username=postgres;Password=<password>
